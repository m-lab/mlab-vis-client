import chai, { expect } from 'chai';
import chaiEnzyme from 'chai-enzyme';

chai.use(chaiEnzyme());

describe('components', () => {
  describe('LineChart', () => {
    it('sample test', () => {
      expect(true).to.equal(true);
    });
  });
});
